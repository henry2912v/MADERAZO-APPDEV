import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'regir, maderazo, marcos-assignment-3',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
