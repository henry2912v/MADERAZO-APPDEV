import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'MaderazoAct5',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
